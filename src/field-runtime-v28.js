const F=document.querySelector('#field');
const MAP={cao:0,dun:1,yuan:2,ren:3,xun:4,inf:5,spear:6,archer:7,zhangbao:8,hua:9,lubu:10,yanliang:11};
let busy=false;
function ensureSprite(el){if(!el)return;const cls=[...el.classList].find(c=>c!=='sprite')||'inf';let img=el.querySelector('.unitSprite');if(!img){img=document.createElement('span');img.className='unitSprite';el.append(img)}const i=MAP[cls]??5;img.style.backgroundPosition=(-i*32)+'px 0px';}
function convertTile(node){if(!node||!node.classList?.contains('tile'))return node;if(node.tagName!=='BUTTON')return node;const d=document.createElement('div');d.className=node.className;d.setAttribute('role','button');d.tabIndex=0;for(const a of [...node.attributes]){if(!['class','style','type'].includes(a.name)&&a.name!=='tabindex')d.setAttribute(a.name,a.value)}while(node.firstChild)d.append(node.firstChild);const click=node.onclick,enter=node.onmouseenter;d.onclick=click;d.onmouseenter=enter;d.addEventListener('keydown',e=>{if((e.key==='Enter'||e.key===' ')&&typeof click==='function'){e.preventDefault();click.call(d,e)}});node.replaceWith(d);return d}
function layout(){if(!F||busy)return;busy=true;const raw=[...F.children];raw.forEach((node,i)=>{const t=convertTile(node);const x=i%14,y=Math.floor(i/14);t.style.position='absolute';t.style.left=(x*100/14)+'%';t.style.top=(y*10)+'%';t.style.width=(100/14)+'%';t.style.height='10%';t.style.margin='0';t.style.padding='0';t.style.border='0';t.style.outline='0';t.style.boxShadow='none';t.style.backgroundColor='transparent';t.querySelectorAll('.sprite').forEach(ensureSprite)});document.querySelectorAll('.portrait .sprite').forEach(ensureSprite);busy=false}
const obs=new MutationObserver(()=>requestAnimationFrame(layout));obs.observe(document.body,{childList:true,subtree:true});addEventListener('resize',layout);layout();setTimeout(layout,30);setTimeout(layout,150);setTimeout(layout,500);
