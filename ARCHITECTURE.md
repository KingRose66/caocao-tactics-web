# 魏武英杰传 · 重构架构约束

本文件是后续实现的唯一架构基准。停止继续在旧版本上叠加补丁。

当前美术基线：**MAP STAGE 11 HQ · 1120×800**。在进入逻辑棋盘之前，地图加载方案与地图构图冻结；后续每次只改一个层级。

## 1. 固定渲染顺序

战场必须严格分成五层，互不承担彼此职责：

1. **Map Art（地图美术层）**
   - 一张连续战场地图。
   - 不绘制棋盘线、不按 14×10 单元切割视觉纹理。
   - 河流、道路、树林、村落、高地等属于地图画面本身。
   - 地图首先服务于“真实战场感”，其次才是装饰；主要交战区域不能因为树木、岩壁、阴影过黑而吞掉兵将。

2. **Logic Grid（逻辑棋盘层）**
   - 14×10，仅存坐标、地形规则、寻路和点击命中。
   - 默认 100% 透明，不允许 border / outline / box-shadow / 背景纹理。
   - 它不是地图，也不承载兵将 DOM。

3. **Range Overlay（范围提示层）**
   - 只有选中武将并进入移动/攻击/技能状态时才创建。
   - 允许出现半透明蓝/红/紫区域。
   - 退出状态后立即销毁，不保留常驻格线。

4. **Unit Layer（兵将层）**
   - 武将、士兵独立绝对定位，不嵌套在逻辑格节点内。
   - 兵将可以自然越出单格边界，以避免“每人被关在一个方框里”的视觉感。
   - 兵将必须优先保证可读性：阵营色、轮廓、脚下阴影、血条和选中框都在本层解决，不能靠常驻网格帮助识别。

5. **FX / UI（特效与界面层）**
   - 伤害数字、技能效果、对话、信息面板等。
   - 不反向修改地图层和逻辑棋盘层。

## 2. 数据与画面分离

- `data.js` 只保存武将、关卡、地形规则与初始坐标。
- 地图美术文件只负责画面，不作为寻路依据。
- 游戏规则通过逻辑坐标访问地形数据，不通过 DOM class 或地图像素猜测规则。
- 点击坐标 -> 逻辑格 -> 游戏规则；视觉层只是结果展示。
- 每关应单独拥有：地图素材、逻辑地形矩阵、我军/敌军出生点、目标、回合上限、AI 角色、剧情触发；禁止把新关卡位置规则硬编码进渲染器。

## 3. 文件纪律

目标结构：

```text
index.html
assets/
  maps/
  units/
css/
  game.css
src/
  data.js
  map-loader.js
  grid.js
  units.js
  combat.js
  game.js
.github/workflows/pages.yml
```

禁止继续新增 `hotfixXX.css`、`stableXX.css`、`visual-vXX.js` 一类补丁文件。旧实验资源确认不再需要后，应集中删除，而不是长期叠加。

## 4. 固定制作顺序

1. **先做地图美术**：只看地图，禁止同时放兵、网格、战斗系统。
2. **冻结地图加载链**：地图能在桌面和 iPad 稳定完整显示后，不因后续功能修改加载器。
3. **加入透明逻辑棋盘**：验证 14×10 坐标、点击、地形映射；默认不得看见格线。
4. **加入范围提示层**：只有移动/攻击/技能选择时才显示可行动区域。
5. **加入真实兵将**：先解决位置、比例、辨识度，再恢复战斗。
6. **恢复战斗系统**：普通攻击、技能、冷却、MP、伤害、AI、胜负。
7. **恢复章节推进与 UI**：对话、人物头像、信息面板、下一关。
8. 最后才做动画、音效和额外装饰。

每一步单独验收，上一层不稳定时绝不进入下一层。

## 5. 地图美术规范

### 5.1 构图

- 地图必须是一张连续场景，而不是由可见方格拼起来。
- 河流、桥梁、道路、村庄、树林、山崖应形成自然的空间关系，并同时服务于关卡玩法。
- 主要交战通道需要有足够留白；高细节树林、岩壁优先放在边缘和阻挡区域。
- 不为“看起来丰富”而让整张地图每处都同样复杂。

### 5.2 色调与兵将可读性

当前 Stage 11 树木和山体暗部偏重，此问题留到 Unit Layer 一并处理，禁止现在反复改动地图加载结构。

加入兵将时按以下顺序解决：

1. 先测试蓝军、红军、黑甲/深蓝甲人物在草地、树林、道路、河岸上的辨识度。
2. 优先调整兵将自身：阵营主色、亮部、1–2px 明暗轮廓、轻微脚下阴影。
3. 如仍不清楚，再降低主要交战区域背景的局部对比/暗部密度；不直接把整张地图粗暴提亮。
4. 选中单位使用经典角标/框，不使用整格黄色边框。
5. 100% 缩放下无需显示网格，也应能立即识别我军、敌军和当前选中单位。

### 5.3 分辨率

- 新地图从一开始就按最终显示尺寸或更高分辨率制作，禁止先做 448×320 再强行放大。
- 当前基线 1120×800；以后新关卡原则上不得低于当前基线。
- 先保存高质量母版，再派生网页版本；母版不能被低质量网页压缩版本反向覆盖。

## 6. 地图资源加载经验（本项目当前连接器环境）

此前反复出现的核心故障不是浏览器，而是大二进制/长文本经连接器提交时出现截断。以后遵守：

- **已经验证稳定的加载链不随美术修改一起重写。**
- 当前通过验证的方案：JPEG -> base64 -> 小分片 -> 浏览器重组 Blob。
- 在当前 ChatGPT/GitHub 连接器环境下，高清地图分片控制在约 **3000 字符/块**；不再尝试超长单块上传。
- 分片必须先在本地计算预期 Git blob SHA，仓库创建后逐块比对 SHA。
- 所有分片、loader、HTML 必须在同一个原子 Git tree/commit 中形成一个完整版本；禁止页面已经切新版而素材只上传一半。
- Loader 至少校验：分片数量、总长度、JPEG 头 `/9j/`、JPEG 尾、浏览器 `img.onload`。
- 如果以后改用正常 Git CLI/Codex 二进制提交，可直接使用真实 JPG/WebP 文件；届时可淘汰分片兼容层，但必须单独迁移和验收，不和新关卡开发同时进行。

## 7. 每次改地图/新关卡的验收清单

每次发布前由开发侧自行验收，不能再依赖用户截图发现基础错误：

- 地图是否完整显示，没有灰块、截断、破图。
- 页面版本号与预期 commit 是否一致。
- 桌面浏览器和 iPad 比例是否正常，无异常裁切。
- 地图是否无常驻网格、无网页按钮感。
- 河流/桥/道路/阻挡区与逻辑地形矩阵是否一致。
- 14×10 逻辑棋盘是否默认完全透明。
- 点击任意四角、中央、桥面坐标是否映射到正确格子。
- 兵将加入后，树林/山体背景是否影响辨识度。
- 移动范围只在需要时显示，结束操作后完全消失。
- 胜负、AI、章节推进修改不能改变地图层。
- GitHub Pages workflow 必须 `completed / success` 后才通知用户刷新。

## 8. 失败经验转化为硬规则

- **不再“头痛医头、脚痛医脚”**：先定位属于 Map / Grid / Unit / Combat / UI 哪一层，只修改该层。
- **不同时改两种未知变量**：例如“换高清素材”时禁止同时重写 loader；“加兵将”时禁止同时重构地图。
- **稳定版本永远可回滚**：大改先分支验收，主分支只接已经验证通过的版本。
- **视觉棋盘与逻辑棋盘永远分离**：逻辑上有格子，不代表玩家必须一直看见格子。
- **地图不是游戏逻辑**：换地图图像不能改变移动、攻击和 AI；游戏逻辑也不能靠修改地图图片来修。
- **先完成基础层，再恢复功能**：地图 -> 隐藏棋盘 -> 兵将 -> 战斗，不再逆序。
