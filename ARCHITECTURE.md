# 魏武英杰传 · 重构架构约束

本文件是后续实现的唯一架构基准。停止继续在旧版本上叠加补丁。

## 1. 固定渲染顺序

战场必须严格分成五层，互不承担彼此职责：

1. **Map Art（地图美术层）**
   - 一张连续战场地图。
   - 不绘制棋盘线、不按 14×10 单元切割视觉纹理。
   - 河流、道路、树林、村落、高地等属于地图画面本身。

2. **Logic Grid（逻辑棋盘层）**
   - 14×10，仅存坐标、地形规则、寻路和点击命中。
   - 默认 100% 透明，不允许 border / outline / box-shadow / 背景纹理。
   - 它不是地图，也不承载兵将 DOM。

3. **Range Overlay（范围提示层）**
   - 只有选中武将并进入移动/攻击/技能状态时才创建。
   - 允许出现半透明蓝/红/紫区域。
   - 退出状态后立即销毁，不保留常驻格线。

4. **Unit Layer（兵将层）**
   - 武将、士兵独立绝对定位，不嵌套在逻辑格节点内。
   - 兵将可以自然越出单格边界，以避免“每人被关在一个方框里”的视觉感。

5. **FX / UI（特效与界面层）**
   - 伤害数字、技能效果、对话、信息面板等。
   - 不反向修改地图层和逻辑棋盘层。

## 2. 数据与画面分离

- `data.js` 只保存武将、关卡、地形规则与初始坐标。
- 地图美术文件只负责画面，不作为寻路依据。
- 游戏规则通过逻辑坐标访问地形数据，不通过 DOM class 猜测规则。
- 点击坐标 -> 逻辑格 -> 游戏规则；视觉层只是结果展示。

## 3. 文件纪律

重构完成后目标结构：

```text
index.html
assets/
  battlefield-yingchuan.svg
  units.svg
css/
  game.css
src/
  data.js
  game.js
.github/workflows/pages.yml
```

禁止继续新增 `hotfixXX.css`、`stableXX.css`、`visual-vXX.js` 一类补丁文件。

## 4. 制作顺序

1. 先完成并验收连续地图，不放兵、不放可见网格。
2. 加透明逻辑棋盘，验证每个位置都可正确点击、寻路；仍然不显示格线。
3. 加范围提示层，验证只有操作时才显示。
4. 加兵将层，验证兵将位置和点击命中一致。
5. 再接攻击、技能、AI、胜负与章节推进。
6. 最后完善经典 SRPG UI、动画与音效。

每一步单独验收，上一层不稳定时不进入下一层。
